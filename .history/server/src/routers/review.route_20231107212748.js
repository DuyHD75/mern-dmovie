import express from 'express';
import reviewContr from '../controllers/person.controller.js'

const router = express.Router({ mergeParams: true });

// CRUD review

router.get();


export default router;