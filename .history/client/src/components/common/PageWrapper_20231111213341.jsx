import React from 'react'
import { useDispatch } from 'react-redux'

const PageWrapper = ({state, chi}) => {

     const dispatch = useDispatch();


     return (
          <div>

          </div>
     )
}

export default PageWrapper;
