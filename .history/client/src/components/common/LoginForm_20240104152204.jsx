import React from 'react'
import {LoadingButton} from '@m'
const LoginForm = ({ switchAuthState }) => {
     return (
          <div>
               LoginForm
          </div>
     )
}

export default LoginForm;
