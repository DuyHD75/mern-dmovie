import React from 'react'
import {LoadingButton} from '@mui/lab';
import {Alert, Box, Button, Stack, TextField} from 



const LoginForm = ({ switchAuthState }) => {
     return (
          <div>
               LoginForm
          </div>
     )
}

export default LoginForm;
